// const crypto = require("crypto");

// function generateRandomID(length, base ) {
//   const bytes = crypto.randomBytes(Math.ceil(length / 2));
//   const Id =  bytes.toString("hex").slice(0, length).toUpperCase()
// }

// module.exports = generateRandomID;
